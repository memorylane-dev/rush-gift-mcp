# Tests

Current test coverage:

- service-level recommendation flow
- impossible timing fallback
- budget filtering
- pickup option matching
- MCP tool registration
- MCP tool structured response shape

Run:

```bash
uv run pytest
```
